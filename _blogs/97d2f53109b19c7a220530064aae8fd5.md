---
layout: article
category: Research
date: 2025-01-22
title: 'What Does the "@" Symbol Do in Python? Explaining Its Two Key Uses in D2L Code'
excerpt: 'Recently, while going through D2L (Dive into Deep Learning) code, I frequently noticed the "@" symbol appearing at the very top of classes or functions. Although Chapter 2 explains the comment `#@save` (indicating the following function is pre-defined in the package and can be used directly without redefinition), it does not explain the usage of the "@" symbol itself.'
originurl: "https://blog.csdn.net/qq_33919450/article/details/145294350"
---
Recently, while going through D2L (Dive into Deep Learning) code, I frequently noticed the "@" symbol appearing at the very top of classes or functions. Although Chapter 2 explains the comment `#@save` (indicating the following function is pre-defined in the package and can be used directly without redefinition), it does not explain the usage of the "@" symbol itself.

<div style="text-align: center;">
  <img alt="D2L code snippet showing @ symbol before a function" src="/assets/images/7ce9fade3d8a41118ce8051a0755e8bd.png" style="box-shadow: 0px 0px 0px 0px; width: 50%;">
</div>

After researching, I found that "@" has two main meanings in Python:
1. Matrix multiplication
2. Decorators for classes and functions

## 1. Matrix Multiplication
In some textbooks, matrix multiplication is calculated using the `matmul` method (e.g., PyTorch’s `torch.matmul`). However, you can directly use "@" to represent matrix multiplication, as shown in this example:

```python
import torch
from torch.utils import benchmark
typ = torch.float16
n = 1024*16
a = torch.randn(n,n).type(typ).cuda()
b = torch.randn(n,n).type(typ).cuda()
t = benchmark.Timer (
    stmt='a @ b', # The "@" here means matrix multiplication
    globals={'a':a,'b':b}
)
x=t.timeit(50)
2*n**3/x.median/1e12
```

>Note: This usage is not exclusive to PyTorch—you’ll find it defined in the official Python documentation as well.<br />

>As an aside, I came across this code in [one of Li Mu’s posts](https://www.bilibili.com/opus/693787692502089749). For reference, the RTX 3060 scores around 28 on this benchmark—you can use it to test your GPU’s FP16 (half-precision floating-point) performance.<br />

<div style="text-align: center;">
  <img alt="FP16 performance benchmark result for RTX 3060" src="/assets/images/2d82753f65764f28be3247b811c02bab.png" style="box-shadow: 0px 0px 0px 0px; width: 80%;">
</div>

## 2. Decorators for Classes and Functions
A decorator is a form of syntactic sugar (designed to reduce code redundancy). It means the class/function below the "@" symbol is passed as an argument to the function following the "@".

For example, the official Python documentation provides this example—these two function definitions are equivalent:

```python
def f(arg):
    ...
f = staticmethod(f)
```

```python
@staticmethod
def f(arg):
    ...
```

As you can see, `f` is the argument to `staticmethod`.

Now let’s return to the code snippet from the start:

<div style="text-align: center;">
  <img alt="D2L code snippet with @d2l.add_to_class decorator" src="/assets/images/7ce9fade3d8a41118ce8051a0755e8bd.png" style="box-shadow: 0px 0px 0px 0px; width: 50%;">
</div>

First, `LinearRegression` is a pre-defined class (not shown in the snippet). Second, `d2l.add_to_class(LinearRegression)` means the following function is added to the `LinearRegression` class as one of its methods.

If this example is still confusing, take a look at these screenshots:

<div style="text-align: center;">
  <img alt="Code showing LinearRegression class instantiation" src="/assets/images/7abbe3ddde2c41a6a3784ea4608158d6.png" style="box-shadow: 0px 0px 0px 0px; width: 30%;">
</div>

<div style="text-align: center;">
  <img alt="Code showing get_w_b() method available after decorator usage" src="/assets/images/4814c4e00a0f4edc9e75797c81de0810.png" style="box-shadow: 0px 0px 0px 0px; width: 50%;">
</div>

You can see that `model` (an instance of the `LinearRegression` class) gains access to the newly added `get_w_b()` method after the decorator is applied.

I hope these will help someone in need~

## References
- [What does the “at” (@) symbol do in Python? - Stack Overflow](https://stackoverflow.com/a/46027116): This answer (not the top-voted one) is exceptionally comprehensive and high-quality—no need to read other responses. It was also the starting point for my research.
- [decorator - Glossary - Python](https://docs.python.org/3/glossary.html#term-decorator): A brief introduction to decorators in the official Python documentation—this is the only standalone section dedicated to decorators; all other mentions are mixed with other topics, as shown below:

<div style="text-align: center;">
  <img alt="Python official documentation snippet about decorators" src="/assets/images/727ad7e4dad44a6fb57d657a241d2415.png" style="box-shadow: 0px 0px 0px 0px; width: 70%;">
</div>